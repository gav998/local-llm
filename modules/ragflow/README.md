# RAGFlow module

Independent native-Windows RAGFlow 0.27.1 API/task module with CPython 3.13.15.
Its online build keeps the existing audited Windows patches, exact asset
revisions and dependency overrides. Runtime integration uses only sibling
`state/connection.json` contracts; no component files are shared.
